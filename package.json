{
  "name": "only-for-workflows",
  "version": "unknown",
  "scripts": {
    "lint": "npm -C package run lint",
    "test": "npm -C package run test"
  },
  "dependencies": {
    "vite": "^2.0.0-beta.34",
    "vite-plugin-ruby": "^1.0.1"
  }
}
